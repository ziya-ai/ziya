{
 "type": "chat-message",
 "definition": "## epoch lease backpressure tombstone checkpoint backpressure backpressure watermark partition scheduler shard checkpoint quorum watermark scheduler replica reconcile tombstone shard quorum compaction partition reconcile epoch quorum scheduler backpressure quorum\n\n| Very long header cell describing the first measured dimension in a full sentence | Second |\n|:---|:---|\n| compaction lease replica checkpoint compaction replica checkpoint checkpoint reconcile replica epoch epoch shard replica replica scheduler scheduler quorum checkpoint quorum replica replica lease tombstone quorum partition watermark watermark quorum replica | backpressure quorum compaction lease scheduler tombstone compaction replica replica lease reconcile tombstone lease epoch |\n| epoch scheduler epoch watermark backpressure tombstone reconcile lease tombstone lease shard backpressure tombstone replica shard shard backpressure replica scheduler lease scheduler backpressure tombstone compaction scheduler partition checkpoint compaction tombstone compaction | epoch scheduler shard scheduler backpressure replica epoch quorum reconcile checkpoint quorum shard tombstone partition |\n| tombstone partition lease checkpoint shard reconcile epoch backpressure checkpoint checkpoint tombstone lease scheduler compaction reconcile quorum tombstone partition epoch tombstone replica tombstone lease backpressure partition reconcile lease watermark tombstone lease | replica checkpoint reconcile watermark replica backpressure backpressure lease shard replica backpressure scheduler reconcile epoch |\n| partition compaction scheduler quorum backpressure epoch tombstone lease shard watermark compaction replica scheduler watermark scheduler checkpoint shard tombstone quorum replica backpressure epoch replica watermark checkpoint compaction reconcile replica compaction tombstone | replica scheduler compaction lease replica shard epoch replica partition shard shard backpressure backpressure tombstone |\n| shard lease lease shard compaction replica reconcile compaction partition replica watermark shard tombstone replica reconcile shard lease partition checkpoint partition partition tombstone reconcile checkpoint checkpoint checkpoint tombstone backpressure epoch watermark | scheduler checkpoint lease tombstone partition backpressure watermark lease shard lease checkpoint backpressure backpressure lease |\n\n- tombstone watermark replica epoch scheduler shard partition checkpoint lease tombstone watermark lease shard lease partition watermark watermark checkpoint tombstone tombstone lease watermark tombstone backpressure compaction tombstone replica backpressure shard tombstone tombstone partition replica partition replica quorum tombstone shard lease backpressure\n- reconcile backpressure watermark backpressure compaction replica epoch checkpoint epoch partition watermark compaction lease epoch partition checkpoint watermark lease backpressure scheduler quorum replica epoch shard epoch watermark replica quorum checkpoint watermark replica watermark backpressure scheduler shard quorum replica scheduler replica reconcile\n\n> tombstone replica shard quorum partition scheduler compaction shard tombstone compaction watermark epoch reconcile epoch quorum quorum backpressure tombstone scheduler tombstone compaction lease compaction reconcile backpressure partition tombstone scheduler partition epoch lease reconcile lease partition partition tombstone epoch lease tombstone replica compaction compaction epoch watermark partition\n",
 "intent": "very long labels: a 28-word heading, a 5-row table with ~30-word cells, two 40-word list items and a 45-word blockquote, probing wrapping of long labels inside constrained containers"
}
