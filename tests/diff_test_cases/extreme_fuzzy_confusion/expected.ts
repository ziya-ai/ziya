// Block A
const toolDisplay = headerCmd 
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;

// Block B
const toolDisplay = headerCmd 
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;

// Block C
const toolDisplay = headerCmd 
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;

// Block D - TARGET
let toolDisplay;
if (headerCmd) {
    toolDisplay = `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`;
} else {
    toolDisplay = `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;
}

// Block E
const toolDisplay = headerCmd
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;

// Block F
const toolDisplay = headerCmd 
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;

// Block G
const toolDisplay = headerCmd 
    ? `\n\`\`\`tool:${name}|${headerCmd}\n${content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${name}\n${content}\n\`\`\`\n\n`;
