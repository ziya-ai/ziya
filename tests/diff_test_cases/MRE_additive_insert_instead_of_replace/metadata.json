{
  "target_file": "src/components/MarkdownRenderer.tsx",
  "description": "MRE for the additive-replace symptom where both old and new lines appear in output. The diff was generated by an AI that wrote INSERTIONS instead of REPLACEMENTS — each hunk has the old key={index} line as context (space-prefixed) and the new key={sk} line as addition (+-prefixed), with NO corresponding '-' removal line. The preprocessor converts these to proper removal+addition pairs.",
  "date_added": "2025-07-14",
  "issue_reference": "additive_insert_instead_of_replace_multi_hunk",
  "bug_class": "additive_insert_instead_of_replace"
}
