diff --git a/src/component.tsx b/src/component.tsx
--- a/src/component.tsx
+++ b/src/component.tsx
@@ -2,7 +2,7 @@
 import { useState, useCallback } from "react";
 
 // Configuration constants
-const MAX_RETRIES = 3;
+const MAX_RETRIES = 5;  // Updated from 3
 const TIMEOUT_MS = 30000;
 
 // Placeholder line 7 for component structure
@@ -51,6 +51,7 @@
 
 export interface ComponentProps {
     title: string;
+    subtitle?: string;  // Added optional subtitle
     onClose: () => void;
 }
 
@@ -99,6 +100,11 @@
 // Implementation detail line 99
 
 const handleSubmit = useCallback(async (data: FormData) => {
+    // Validate input before submission
+    if (!data.isValid()) {
+        console.warn("Invalid form data");
+        return { success: false, error: "validation_failed" };
+    }
     try {
         const result = await submitForm(data);
         return { success: true, data: result };
@@ -175,16 +181,26 @@
 
 const contextEnhancementOverlay = (isCheckingFiles || needsContextEnhancement) ? (
     <div style={{
-        position: "relative", width: "100%",
-        backgroundColor: needsContextEnhancement ? "rgba(255,193,7,0.9)" : "rgba(0,0,0,0.7)",
-        color: needsContextEnhancement ? "#000" : "white",
-        padding: "12px", textAlign: "center",
+        position: "relative",
+        width: "100%",
+        backgroundColor: needsContextEnhancement
+            ? (autoAddDiffFiles
+                ? (isDarkMode ? "rgba(23,125,220,0.15)" : "rgba(24,144,255,0.08)")
+                : "rgba(255,193,7,0.9)")
+            : "rgba(0,0,0,0.7)",
+        color: needsContextEnhancement
+            ? (autoAddDiffFiles
+                ? (isDarkMode ? "#7ec8f2" : "#096dd9")
+                : "#000")
+            : "white",
+        padding: "10px 14px",
+        textAlign: "center",
         borderRadius: "4px"
     }}>
         {isCheckingFiles ? (
             "Checking context..."
         ) : needsContextEnhancement ? (
-            <span>Files not in context</span>
+            <span>Auto-added files to context</span>
         ) : null}
     </div>
 ) : null;
