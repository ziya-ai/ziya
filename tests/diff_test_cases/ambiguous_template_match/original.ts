// Block 1
const toolResultDisplay = headerCommand 
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n${formatted.content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${toolName}\n${formatted.content}\n\`\`\`\n\n`;

const toolStartPrefix = headerCommand
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n`
    : `\n\`\`\`tool:${toolName}\n`;

// Block 2
const toolResultDisplay = headerCommand 
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n${formatted.content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${toolName}\n${formatted.content}\n\`\`\`\n\n`;

const toolStartPrefix = headerCommand
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n`
    : `\n\`\`\`tool:${toolName}\n`;

// Block 3
const toolResultDisplay = headerCommand 
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n${formatted.content}\n\`\`\`\n\n`
    : `\n\`\`\`tool:${toolName}\n${formatted.content}\n\`\`\`\n\n`;

const toolStartPrefix = headerCommand
    ? `\n\`\`\`tool:${toolName}|${headerCommand}\n`
    : `\n\`\`\`tool:${toolName}\n`;
