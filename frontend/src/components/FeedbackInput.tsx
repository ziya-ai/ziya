// This component is now deprecated - tool feedback is handled by SendChatContainer
// Keeping file for potential future use or as reference

import React, { useState, useEffect, useRef } from 'react';
import { Input, Button, Card, Typography } from 'antd';
import { SendOutlined, CloseOutlined } from '@ant-design/icons';

// Deprecated component - functionality moved to SendChatContainer
const FeedbackInput: React.FC<any> = () => null;

export default FeedbackInput;
